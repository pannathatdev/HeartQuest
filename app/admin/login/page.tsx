"use client";
import { useState } from "react";
export default function AdminLogin(){const [password,setPassword]=useState(""),[error,setError]=useState("");const login=async(e:React.FormEvent)=>{e.preventDefault();const response=await fetch("/api/admin/login",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({password})});if(response.ok)location.href="/admin/payments";else setError("รหัสผ่านไม่ถูกต้อง")};return <main className="admin-login"><form onSubmit={login}><div className="success-icon">♥</div><p className="eyebrow">HEARTQUEST ADMIN</p><h1>เข้าสู่ระบบหลังบ้าน</h1><label>รหัสผ่านผู้ดูแล<input type="password" value={password} onChange={e=>setPassword(e.target.value)} autoFocus/></label>{error&&<p className="pay-error">{error}</p>}<button className="primary-button full">เข้าสู่ระบบ</button></form></main>}
