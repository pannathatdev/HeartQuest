import { env } from "cloudflare:workers";
import { premiumPackages } from "../../../lib/packages";
export async function POST(request:Request){const body=await request.json() as {slug?:string;packageId?:string;contact?:string};const pack=premiumPackages[body.packageId||""];if(!pack)return Response.json({error:"แพ็กเกจไม่ถูกต้อง"},{status:400});const game=await env.DB.prepare("SELECT id FROM games WHERE slug = ?").bind(String(body.slug||"")).first<{id:string}>();if(!game)return Response.json({error:"ไม่พบเกม"},{status:404});const id=crypto.randomUUID();await env.DB.prepare("INSERT INTO payments (id, game_id, package_id, amount_satang, provider, status, customer_contact, created_at) VALUES (?, ?, ?, ?, 'manual_promptpay', 'awaiting_payment', ?, ?)").bind(id,game.id,body.packageId,pack.amountSatang,String(body.contact||"").slice(0,120)||null,Date.now()).run();return Response.json({id,amount:pack.amountSatang});}
