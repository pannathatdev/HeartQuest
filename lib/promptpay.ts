const field=(id:string,value:string)=>`${id}${String(value.length).padStart(2,"0")}${value}`;
function crc16(value:string){let crc=0xffff;for(let i=0;i<value.length;i++){crc^=value.charCodeAt(i)<<8;for(let bit=0;bit<8;bit++)crc=(crc&0x8000)?(crc<<1)^0x1021:crc<<1;crc&=0xffff}return crc.toString(16).toUpperCase().padStart(4,"0")}
export function promptPayPayload(phone:string,amountSatang:number){const digits=phone.replace(/\D/g,"");const target=digits.startsWith("0")?`0066${digits.slice(1)}`:digits;const merchant=field("00","A000000677010111")+field("01",target);let payload=field("00","01")+field("01","12")+field("29",merchant)+field("53","764")+field("54",(amountSatang/100).toFixed(2))+field("58","TH")+field("59","HEARTQUEST")+field("60","BANGKOK")+"6304";return payload+crc16(payload)}
